streamlit>=1.32.0
yt-dlp
pytube>=15.0.0

# Local transcription with Whisper (will install PyTorch and related packages)
# Note: installing these on first deploy can be slow and large. Consider using a
# smaller Whisper model (e.g. "tiny") in `app.py` to reduce memory and CPU usage.
openai-whisper>=20231117
torch>=2.2.0
torchaudio>=2.2.0
librosa>=0.10.1
soundfile>=0.12.1
ffmpeg-python>=0.2.0
python-dotenv>=1.0.0

# Fetch captions without media download and perform simple HTTP requests
youtube-transcript-api>=0.6.2
requests>=2.31.0

# Optional: add `openai` if you later want cloud fallback
# openai>=1.0.0
 